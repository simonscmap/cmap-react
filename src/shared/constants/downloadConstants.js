// Global download configuration constants
// Used by multiple features that need to enforce download limits

export const DOWNLOAD_LIMITS = {
  // Maximum row count allowed for bulk downloads (3 million rows)
  MAX_ROW_THRESHOLD: 3000000,
};
