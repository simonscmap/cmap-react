let hint = `
  <div>
    <p style="text-align:justify;">
      Filter datasets by their type ('Makes'), production method ('Sensors'),
      regions, or by definig a spatio-temporal bounding box (see 'Additional
      Filters').
    </p>
  </div>`;

export default hint;
