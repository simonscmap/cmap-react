import React from 'react';
import HintContent from '../../Help/HintContent';

export default () => (
  <HintContent>
    <h3>Add Dataset to Favorites</h3>
    <p>
      Selecting the star-shaped “add to favorites” icon for a given dataset adds
      that dataset to the favorites list which can be accessed in the upper
      right corner of the screen. Datasets selected as ‘favorites’ will also
      appear at the top of the list on the data visualization page, supporting
      efficient plotting of these datasets.
    </p>
  </HintContent>
);
